<%
  ['created_at', 'updated_at'].each do |attr|
    row.column :html => {class: 'fdatetime'}, :name => model.human_attribute_name(attr),
      :attribute => attr do |instance|
        instance[attr].nil? ? '' : l(instance[attr], format: :fdatetime)
    end
  end
%>
